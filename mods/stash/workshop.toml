slug = "stash"
title = "Stash"
description = """
Stash is a BaronyModLoader reference mod for a shared Void Chest-style stash. BML is becoming multi-platform, starting with fail-closed Windows scaffolding, but Stash production behavior is currently verified only on the Steam/Linux installed-binary hook path.

Current requirement:
- requires BML to launch the installed Steam/Linux Barony executable with the validated libbarony_bml.so hook runtime and jml.stash runtime manifest for the currently verified production path
- is not a Workshop-only data mod; current behavior comes from the BML installed-binary hook runtime, with old source-fork artifacts retained only as design references
- includes metadata for the Windows barony.exe + windows-createprocess-loadlibrary + barony_bml.dll contract with canonical bml-win-launcher.exe launcher naming; buildable scaffold/self-test artifacts are not Windows gameplay evidence, and Windows remains unverified until production artifact metadata and live Windows evidence exist
- live Steam/Linux evidence currently verifies the Start Map lobby access point, lid, and Open stash prompt
- fake-provider evidence covers production hook installation, generated-shop placement logic, shared inventory/spell binding, metadata guard reporting, and prompt scoping
- live gameplay still needs verification for real generated-shop placement, cross-run persistence, player-cast spell-created Void Chests, save/resume, disabled behavior, and multiplayer mismatch rejection

Keep hidden until the BML installed-hook runtime and remaining live player-facing scenarios are ready for public release; do not advertise Windows gameplay until the Windows runtime gates pass with production barony_bml.dll metadata, the canonical bml-win-launcher.exe launcher, a matching hook/build manifest, and live Windows evidence.
"""
preview = "preview.png"
visibility = 2
publishedfileid = "0"
changenote = "Hidden BML platform metadata update: Steam/Linux remains verified; Windows scaffold builds are fail-closed/self-test-only until live runtime evidence"

[content]
folder = "content"
